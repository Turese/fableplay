<p id="notice"><%= notice %></p>

<h1>Listing Fablepets</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Unique</th>
      <th>Species</th>
      <th>Pattern</th>
      <th>Colors</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fablepets.each do |fablepet| %>
      <tr>
        <td><%= fablepet.name %></td>
        <td><%= fablepet.unique_id %></td>
        <td><%= fablepet.species %></td>
        <td><%= fablepet.pattern %></td>
        <td><%= fablepet.colors %></td>
        <td><%= link_to 'Show', fablepet %></td>
        <td><%= link_to 'Edit', edit_fablepet_path(fablepet) %></td>
        <td><%= link_to 'Destroy', fablepet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fablepet', new_fablepet_path %>
